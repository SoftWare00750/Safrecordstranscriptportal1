# Local dev values — this file is gitignored and never committed.
# See .env.example for what each variable does.

VITE_SUPABASE_URL=https://zpbzyejalapmnoovsgqs.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_RxMHqu1F9o0bLbiFxpEPKA_IOLenMeW

# Backend is now a Supabase Edge Function (see /supabase/functions/api),
# not the old Express server on :4000 — set this in every environment
# (local dev included), there is no more dev proxy to fall back to.
VITE_API_BASE_URL=https://zpbzyejalapmnoovsgqs.supabase.co/functions/v1/api
